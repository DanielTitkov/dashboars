env: dev
db:
  uri: "host=localhost port=5432 user=test dbname=crudapi password=pass sslmode=disable"
  driver: postgres
server:
  port: 8000
  host: "0.0.0.0"
data:
  presets:
    tasksPresetsPath: ./data/presets/tasks.json